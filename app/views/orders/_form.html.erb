<%= simple_form_for([@shot, @shot.orders.build]) do |f| %>

	<div class="field">
		<div class="control">
			<%= f.input :email, input_html: { class: 'input'}, wrapper: false, label_html: { class: 'label' } %>
		</div>
	</div>
	
	<%= f.button :submit, 'Add comment', class: 'button is-primary' %>
<% end %>